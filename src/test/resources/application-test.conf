env_marker = "TEST CONFIGURATION"

app {
  version = "1.0 - test"
  host = "https://localhost:8080"
  apiUrl = "/api/v1"
}

db {
    user = "test-db-user"
    password = ""
    driver = "org.h2.Driver"
    url = "jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;"
    maximumPoolSize = "5"
}
